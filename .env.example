PORT=8787
# Comma-separated list of allowed browser origins for the marketing site
FRONTEND_ORIGIN=http://localhost:3000,https://howethstudio.com
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/football_era
ADMIN_API_KEY=replace-with-a-long-random-secret
ADMIN_DASHBOARD_USER=developer
ADMIN_DASHBOARD_PASSWORD=replace-with-a-different-long-random-secret
TRUST_PROXY=1
# Native identity-token audiences. Apple uses the iOS bundle identifier; Google
# uses the Web OAuth client ID configured in the app. Multiple Google audiences
# may be comma-separated.
APPLE_CLIENT_ID=com.footballera.game
APPLE_TEAM_ID=replace-with-apple-team-id
APPLE_KEY_ID=replace-with-sign-in-with-apple-key-id
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nreplace-me\\n-----END PRIVATE KEY-----"
GOOGLE_OAUTH_CLIENT_IDS=replace-with-google-web-client-id.apps.googleusercontent.com
# Long random server secret used to encrypt Apple's refresh token before storage.
AUTH_ENCRYPTION_KEY=replace-with-at-least-32-random-characters
# Keep true through the account-enabled app rollout, then set false to retire
# anonymous v1 leaderboard reads and writes with HTTP 410.
LEGACY_LEADERBOARDS_ENABLED=true

# Elevenward has a separate PostgreSQL schema and route namespace but shares the
# same Railway service/database. Its native identity audiences must not reuse
# Football Era's bundle/client IDs.
ELEVENWARD_APPLE_CLIENT_ID=com.howethstudio.elevenward
ELEVENWARD_GOOGLE_OAUTH_CLIENT_IDS=replace-with-elevenward-google-web-client-id.apps.googleusercontent.com
REVENUECAT_WEBHOOK_SECRET=replace-with-a-long-random-revenuecat-authorization-secret

# Ed25519 private key used only by the staff content publisher. Ship the paired
# public key in the app; never ship this private key.
ELEVENWARD_CONTENT_SIGNING_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nreplace-me\\n-----END PRIVATE KEY-----"

# Railway-compatible S3 object bucket for immutable signed content bundles.
ELEVENWARD_CONTENT_BUCKET=elevenward-content
ELEVENWARD_CONTENT_ENDPOINT=https://replace-with-bucket-endpoint
ELEVENWARD_CONTENT_REGION=auto
ELEVENWARD_CONTENT_ACCESS_KEY_ID=replace-me
ELEVENWARD_CONTENT_SECRET_ACCESS_KEY=replace-me
ELEVENWARD_CONTENT_PUBLIC_BASE_URL=https://replace-with-public-content-origin
ELEVENWARD_CONTENT_FORCE_PATH_STYLE=true
